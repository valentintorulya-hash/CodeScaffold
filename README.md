# � Прогнозирование акций SBER.ME (ARIMA-LSTM)

Дипломная работа по дисциплине «Прикладная информатика». Веб-приложение для анализа исторических данных и прогнозирования стоимости акций ПАО «Сбербанк» (SBER.ME) с использованием гибридной модели машинного обучения (ARIMA + LSTM).

![Project Status](https://img.shields.io/badge/Status-Active-success)
![Next.js](https://img.shields.io/badge/Next.js-16.1-black)
![Python](https://img.shields.io/badge/Python-3.8+-blue)
![License](https://img.shields.io/badge/License-MIT-green)

## ✨ Возможности

- **📊 Анализ данных**: Загрузка исторических данных с МосБиржи (MOEX API), визуализация графиков цен (Close/Average).
- **🧠 Гибридная модель**:
  - **ARIMA**: Моделирование линейных трендов и сезонности.
  - **LSTM**: Нейронная сеть для захвата нелинейных остаточных зависимостей.
  - **Ensemble**: Взвешенное объединение результатов для повышения точности.
- **📉 Сравнение метрик**: Автоматический расчет MAE, RMSE, MAPE, R² для каждой модели.
- **� Прогнозирование**: Генерация прогноза на 30+ дней вперед с доверительными интервалами.
- **🧪 Тесты стационарности**: Проверка временных рядов тестами Дики-Фуллера (ADF) и KPSS.

## �️ Технологический стек

### Фронтэнд (Next.js)
- **Framework**: [Next.js 16](https://nextjs.org/) (App Router)
- **Language**: TypeScript 5
- **Styling**: [Tailwind CSS 4](https://tailwindcss.com/) + [shadcn/ui](https://ui.shadcn.com/)
- **Charts**: [Recharts](https://recharts.org/) (интерактивные графики с зумом и панорамированием)
- **Icons**: Lucide React

### Бэкэнд & ML
- **API Орекстрация**: Next.js API Routes (`src/app/api/ml`)
- **ML Движок**: Python 3 (`scripts/ml_backend.py`)
- **Библиотеки**:
  - `pandas`, `numpy` — обработка данных
  - `statsmodels` — ARIMA
  - `tensorflow` / `keras` — LSTM
  - `scikit-learn` — метрики и препроцессинг
- **База данных**: Prisma (SQLite) — для хранения пользовательских данных (опционально)
- **Сервер**: Caddy (в production-окружении)

## 🚀 Установка и запуск

### 🏁 Быстрый старт (Windows)

Для автоматической установки зависимостей и запуска проекта просто запустите файл `start.bat` из корня папки.

### 🛠️ Ручная установка

Для работы приложения требуются **Node.js** (рекомендуется [Bun](https://bun.sh/)) и **Python 3**.

### 1. Установка зависимостей

**Фронтэнд:**
```bash
bun install
# или
npm install
```

**ML Бэкэнд (Python):**
Убедитесь, что у вас установлен Python 3.
```bash
pip install -r scripts/requirements.txt
```

### 2. Запуск в режиме разработки

```bash
bun dev
```
Откройте [http://localhost:3000](http://localhost:3000) в браузере.
*При выполнении анализа Next.js автоматически запустит Python-скрипт.*

### 3. Сборка для Production

```bash
# Сборка Next.js приложения
bun run build

# Запуск standalone-сервера
bun start
```

## 📁 Структура проекта

```text
.
├── prisma/                # Схема базы данных (SQLite)
├── public/                # Статические файлы
├── scripts/               # Python-скрипты и утилиты запуска
│   ├── ml_backend.py      # Основной ML-скрипт (ARIMA+LSTM)
│   ├── ml_service.py      # FastAPI сервис (опционально)
│   ├── requirements.txt   # Python зависимости
│   └── start-standalone.mjs # Скрипт запуска сервера
├── src/
│   ├── app/
│   │   ├── api/ml/        # API Routes (мост между Next.js и Python)
│   │   └── page.tsx       # Главная страница дашборда
│   ├── components/ui/     # Компоненты интерфейса (shadcn/ui)
│   └── lib/               # Утилиты (API клиенты, графики)
├── next.config.ts         # Конфигурация Next.js
└── package.json           # Зависимости проекта
```

## ⚙️ Конфигурация

Приложение поддерживает настройку через переменные окружения (или `.env`):

| Переменная | Описание | Значение по умолчанию |
|------------|----------|-----------------------|
| `ALLOW_PYTHON_FALLBACK` | Разрешить запуск `ml_backend.py` через child_process | `true` |
| `USE_FASTAPI_SERVICE` | Пытаться использовать внешний FastAPI сервис | `true` |
| `ML_SERVICE_URL` | URL внешнего ML-сервиса | `http://127.0.0.1:8000` |
| `DATABASE_URL` | Строка подключения к БД | `"file:./dev.db"` |

## 📝 Лицензия

MIT License. Свободное использование в образовательных целях.
